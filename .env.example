# Copy to .env before running locally
# cp .env.example .env

# Local or remote RPC endpoint for Ethereum
RPC_URL=http://localhost:8545

# Network selection (e.g., Ethereum, Rinkeby, or other test networks)
NETWORK=development

# Optional API key for external services
API_KEY=

# Logging level (info | debug | warn)
LOG_LEVEL=info
